<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chinelaegancho.beerdelivery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeerDelivery">
        <activity android:name=".TarjetaActivity"></activity>
        <activity android:name=".PaypalActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".HeinekenPagoActivity" />
        <activity android:name=".MillerPagoActivity" />
        <activity android:name=".CoronaPagoActivity" />
        <activity android:name=".MoropotentePagoActivity" />
        <activity android:name=".MaestroPagoActivity" />
        <activity android:name=".FrostPagoActivity" />
        <activity android:name=".ClasicaPagoActivity" />
        <activity android:name=".ToñaPagoActivity" />
        <activity android:name=".PhoneActivity" />
        <activity android:name=".VerifyActivity" />
        <activity android:name=".MillerActivity" />
        <activity android:name=".HeinekenActivity" />
        <activity android:name=".CoronaActivity" />
        <activity android:name=".MoropotenteActivity" />
        <activity android:name=".MaestroActivity" />
        <activity android:name=".FrostActivity" />
        <activity android:name=".ClasicaActivity" />
        <activity android:name=".TonaActivity" />
        <activity android:name=".BeerMainActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_api_key" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
    </application>

</manifest>